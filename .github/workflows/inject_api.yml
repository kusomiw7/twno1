name: Inject API (Safe)

on:
  workflow_dispatch:
    inputs:
      memory_key:
        description: "記憶鍵值 (例如: system_status)"
        required: false
        default: "system_status"
      memory_content:
        description: "要存入的內容 (例如: 發大財)"
        required: false
        default: "5.2 記憶連結測試成功"

  push:
    branches:
      - main
    paths:
      - "scripts/**"
      - ".github/workflows/inject_api.yml"

permissions:
  contents: read

jobs:
  inject:
    name: Execute 5.2 Memory Injection
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run Injection Script
        shell: bash
        env:
          # 直接從 Secrets 抓取
          API_TOKEN: ${{ secrets.API_TOKEN }}
          API_BASE_URL: ${{ secrets.API_BASE_URL }}
          # 從手動輸入框抓取
          MEMORY_KEY: ${{ github.event.inputs.memory_key }}
          MEMORY_CONTENT: ${{ github.event.inputs.memory_content }}
        run: |
          set -euo pipefail
          if [[ -f "scripts/inject_api.py" ]]; then
            echo "🚀 5.2 引擎啟動：正在注射記憶至 Render..."
            python scripts/inject_api.py
          else
            echo "❌ 找不到 scripts/inject_api.py！請確認檔案位置。"
            exit 1
          fi
