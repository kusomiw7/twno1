name: Inject API (Safe)

on:
  workflow_dispatch:
    inputs:
      memory_key:
        description: "記憶鍵值 (例如: user_pref)"
        required: false
        default: "system_status"
      memory_content:
        description: "記憶內容 (要存入的訊息)"
        required: false
        default: "5.2 系統連線測試成功"
      python_version:
        description: "Python 版本"
        required: false
        default: "3.12"

  push:
    branches:
      - main
    paths:
      - ".github/workflows/inject_api.yml"
      - "scripts/**"

permissions:
  contents: read

jobs:
  inject:
    name: Inject API secrets into runner env
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ github.event.inputs.python_version || '3.12' }}
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run Injection Script
        shell: bash
        env:
          # 從 Secrets 抓取黃金數據
          API_TOKEN: ${{ secrets.API_TOKEN }}
          API_BASE_URL: ${{ secrets.API_BASE_URL }}
          # 從手動輸入抓取記憶內容
          MEMORY_KEY: ${{ github.event.inputs.memory_key || 'system_status' }}
          MEMORY_CONTENT: ${{ github.event.inputs.memory_content || '自動觸發連線成功' }}
        run: |
          set -euo pipefail
          if [[ -f "scripts/inject_api.py" ]]; then
            echo "🚀 啟動記憶注射針..."
            python scripts/inject_api.py
          else
            echo "❌ 錯誤: 找不到 scripts/inject_api.py，請確認檔案路徑是否正確。"
            exit 1
          fi
