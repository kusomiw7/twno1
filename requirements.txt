# Runtime dependencies (server.py)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
gunicorn>=21.2,<23.0

pydantic-settings>=2.2,<3.0
prometheus-client>=0.20,<1.0

# Common CI / workflow utilities (inject_api.yml 常見會用到)
PyYAML>=6.0.1,<7.0
requests>=2.31,<3.0

# Optional but frequently useful for HTTP testing / CI scripts
httpx>=0.27,<1.0
